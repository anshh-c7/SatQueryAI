# SatQuery AI backend.
#
# These are MINIMUM versions known to support Qwen2.5-VL and 4-bit PEFT serving.
# The authoritative set is whatever your Kaggle run used -- capture it there with:
#     !pip freeze | grep -Ei "torch|transformers|peft|bitsandbytes|accelerate"
# and pin those exact versions if you want byte-identical behaviour.
#
# DO NOT INSTALL torchao. peft 0.20's is_torchao_available() raises ImportError
# on torchao < 0.16.0, which breaks adapter loading. Quantisation here is
# bitsandbytes NF4. If it arrives as a transitive dep: pip uninstall -y torchao

torch>=2.4
transformers>=4.49          # Qwen2.5-VL support; 5.x also works (app handles the
                            # torch_dtype -> dtype rename at runtime)
peft>=0.15
bitsandbytes>=0.43
accelerate>=0.34

fastapi>=0.110
uvicorn>=0.29
starlette>=0.37
python-multipart>=0.0.9     # required for Form(...) / File(...) uploads

rasterio>=1.3               # GeoTIFF + SAR band reading
pillow>=10.0
numpy>=1.26
