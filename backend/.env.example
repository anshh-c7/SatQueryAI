# Optional answer-formatting layer. Set this in the backend runtime environment.
# Do not commit the real secret.

# Optional lightweight mode for this machine when the GPU/model runs elsewhere.
# MODEL_BACKEND_URL=https://your-model-device.example.com

# Enables the explicit faculty debug fixtures.
DEBUG_FIXTURES=false

# Optional adapter overrides.
# ADAPTER_A_PATH=./artifacts/adapter_a
# ADAPTER_B_PATH=./artifacts/adapter_b
